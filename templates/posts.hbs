<!DOCTYPE html>
<html>
    <head>

         <meta name="color-scheme" content="dark light">
  
        <style>

            body {
                font-family: sans-serif;
                font-size: medium;
                margin: 0;
                line-height: 1.75;
            }

            h1 {
                font-size: 2.5em;
                font-weight: bold;
                margin-top: 0.5em;
                margin-bottom: 0.05em;
            }

            p.description {
                font-size: 1.5em;
                margin-bottom: 0em;
                margin-top: 0em;
            }

            p.date {
                font-size: 1.0em;
                color: #4d4949;
                margin-bottom: 1.5em;
                font-style: italic;
            }

            a {
                color: inherit;
            }
            a:hover {
                opacity: 0.7;
            }

            .home-link {
                position: fixed;  /* Fixed position */
                top: 5px;     /* Place the link at the top */
                left: 10px;       /* And slightly from the left */
                padding: 5px 10px;  /* Some padding to space out the text */
                z-index: 999;      /* Make sure it appears on top */
            }

        </style>

    </head>
    <body>
         <a href="/index.html" class="home-link">Kirima</a>
        <h1>{{post_title}}</h1>
        <p class="description">{{post_description}}</p>
        <p class="date">{{post_date}}</p>
        <div>
            {{{post_content}}}
        </div>
    </body>
</html>